# 2026 土地銀行 AI 人員考試：科目三機器學習申論題 36 題詳解

> 使用方式：每題先自己用 5-7 點架構作答，再對照詳解。  
> 答題目標：把附件 PDF 的「機器學習技術與應用」內容，轉成土地銀行智能客服、RPA、大數據授信、智能理財、防詐與治理等金融實務場景。

---

## 第一部分：機率統計與銀行 AI 應用

### 1. 請說明機器學習為何可視為學習條件機率 P(Y|X)，並以土地銀行智能客服為例說明。

**答題方向：** 條件機率、分類任務、NLP 意圖辨識、模型信心、人工轉接。

**詳細原理與解答：**

機器學習的核心不是尋找絕對規則，而是在觀察輸入特徵 X 後，推估目標 Y 發生的可能性，也就是 P(Y|X)。在分類任務中，Y 代表類別；在迴歸任務中，Y 代表連續值；在智能客服中，Y 可代表客戶問題的意圖類別，例如「信用卡帳單」、「匯率查詢」、「房貸試算」、「分行查詢」等。

以土地銀行智能客服為例，客戶輸入一句自然語言，例如「我想知道今天美金匯率」，模型會先將文字轉成特徵向量 X，再估計各意圖類別的機率：P(匯率查詢|文字)、P(信用卡帳單|文字)、P(房貸試算|文字)。若「匯率查詢」機率最高，系統就導向匯率查詢服務。

但金融客服不能只看最高機率，還要看信心程度。如果最高機率仍偏低，代表模型不確定，應轉人工客服或要求客戶補充資訊。這就是機器學習在銀行場景中「預測 + 不確定性管理」的價值。

高分答案要補充：智能客服應保留對話紀錄、錯誤回覆案例與人工修正結果，作為後續模型再訓練資料，形成持續改善循環。

---

### 2. 請比較離散型與連續型機率分佈，並各舉兩個銀行 AI 應用例子。

**答題方向：** PMF、PDF、Bernoulli、Poisson、Normal、Exponential。

**詳細原理與解答：**

離散型隨機變數的取值是有限或可數的，通常用機率質量函數 PMF 描述。例如是否違約只有 0 與 1，是否點擊行銷訊息也是 0 與 1，因此可用伯努利分佈建模。若要描述一天內客服來電次數、ATM 異常次數或某時段交易警示數，則可用泊松分佈，因為它適合描述固定時間內事件發生次數。

連續型隨機變數的取值是連續實數，通常用機率密度函數 PDF 描述。例如房屋鑑價誤差、客戶月收入、交易金額等可能近似連續分佈，可視情況用常態分佈建模。若要描述兩次客服來電之間的等待時間、系統故障間隔或客戶下一次交易的時間間隔，可考慮指數分佈。

銀行 AI 作答時不能只寫定義，應說明分佈會影響模型假設。例如邏輯迴歸通常假設二元標籤服從伯努利分佈；線性迴歸常假設誤差項近似常態分佈；客服到訪或異常事件數量則可能用泊松模型輔助分析。

---

### 3. 請用貝氏定理說明金融防詐模型如何根據新證據更新風險判斷。

**答題方向：** 先驗、似然、邊際、後驗、風險更新。

**詳細原理與解答：**

貝氏定理用來描述「在觀察到新證據後，如何更新某事件發生機率」。其核心形式為：後驗機率 = 似然 × 先驗機率 / 邊際機率。

以防詐為例，假設某帳戶原本發生詐欺的先驗機率 P(詐欺) 很低。但當系統觀察到新裝置登入、登入地點異常、短時間內多筆轉帳、收款帳戶為新帳戶等證據後，會計算這些證據在詐欺情況下出現的可能性 P(證據|詐欺)，並更新為 P(詐欺|證據)。

若後驗機率高於某個門檻，系統可採取分級處置，例如提醒客戶、要求二次驗證、暫緩交易或轉交人工風控。金融場景中，貝氏定理的價值在於它能把「原本風險」與「新觀察到的異常行為」整合成可解釋的風險分數。

高分補充：若資料高度不平衡，詐欺先驗機率通常很低，因此模型不應只看 accuracy，而應重視 recall、precision、PR-AUC 與人工覆核成本。

---

### 4. 土地銀行若導入新版智能客服，如何用假設檢定判斷是否真的改善服務品質？

**答題方向：** H0/H1、A/B testing、p-value、顯著水準、指標。

**詳細原理與解答：**

可以設計 A/B 測試，比較舊版客服與新版客服的服務品質。首先設定虛無假設 H0：新版智能客服與舊版在平均問題解決率上沒有差異。對立假設 H1：新版智能客服的平均問題解決率高於舊版。

接著將客戶流量隨機分配到兩組，A 組使用舊版，B 組使用新版。可以觀察的指標包括自助解決率、平均處理時間、轉人工比例、客戶滿意度、錯答率等。若指標是比例，例如解決率，可使用比例檢定；若是平均等待時間，可考慮 t 檢定；若是類別分佈，可使用卡方檢定。

最後計算 p-value，若 p-value 小於顯著水準 alpha，例如 0.05，就可拒絕 H0，認為新版客服有統計上顯著改善。但若 p-value 大於 0.05，不能直接說新版無效，只能說目前資料不足以證明新版有顯著改善。

金融應用還要補充：即使統計上顯著，也要檢查是否具備業務意義，例如客服成本是否下降、客訴是否增加、錯答是否涉及金融法遵風險。

---

### 5. 平均數、中位數、變異數、標準差、偏態與峰度在銀行資料分析中有何用途？

**答題方向：** 描述性統計、異常值、資料分佈、特徵工程。

**詳細原理與解答：**

描述性統計是資料分析與特徵工程的第一步。平均數可描述中心趨勢，例如平均交易金額或平均客服處理時間；中位數較不受極端值影響，適合用於偏態嚴重的金融資料，例如收入與交易金額。變異數與標準差衡量資料分散程度，可用來判斷特徵是否需要標準化，也可偵測異常波動。

偏態用來判斷分佈是否對稱。銀行交易金額通常右偏，少數大額交易會拉高平均數，因此常需要 log transformation。峰度可用來觀察厚尾現象，金融風險資料常有厚尾，代表極端事件雖少但影響大。

在建模上，這些統計量可協助決定是否要處理異常值、是否需標準化、是否需轉換資料分佈，以及是否需要採用對極端值較穩健的損失函數，如 MAE 或 Huber loss。

---

## 第二部分：線性代數、降維與模型表示

### 6. 請說明向量與矩陣在機器學習中的角色，並以授信風險評分為例。

**答題方向：** 特徵向量、特徵矩陣、權重、線性模型。

**詳細原理與解答：**

在機器學習中，一筆樣本通常表示為一個特徵向量，多筆樣本組合成特徵矩陣。以授信風險評分為例，一位企業戶可以表示為向量 x = [成立年數、資本額、產業別、營收、負債比、稅籍狀態、過去繳款紀錄]。若有 n 位客戶、d 個特徵，整體資料可表示為 n × d 的矩陣 X。

模型參數也可表示為權重向量 w。線性模型會計算 w^T x + b，得到一個風險分數，再透過 sigmoid 函數轉成違約機率。神經網路則進一步把每層權重表示為矩陣，透過矩陣乘法與非線性激活函數學習更複雜的關係。

因此，向量與矩陣不是抽象數學，而是銀行 AI 系統表示客戶、交易、文件與模型參數的基本語言。

---

### 7. 請說明 PCA 的原理，並說明它如何用於銀行客戶資料分析。

**答題方向：** 最大變異方向、特徵值分解、降維、去噪、視覺化。

**詳細原理與解答：**

PCA 主成分分析是一種線性降維方法，核心目標是找出資料中變異最大的正交方向，將原始高維資料投影到較低維空間，同時盡量保留主要資訊。數學上，PCA 通常透過對協方差矩陣做特徵值分解，找出特徵值最大的方向作為主成分。

在銀行客戶資料中，可能有數十到數百個欄位，例如交易頻率、收入、負債、產品持有數、通路使用習慣、客服互動紀錄等。若直接使用所有特徵，可能產生共線性、過擬合與計算成本問題。PCA 可將這些高度相關的特徵壓縮成少數主成分，用於客戶分群、視覺化、異常偵測或作為後續模型的輸入。

限制是 PCA 轉換後的主成分不一定容易解釋，且它只捕捉線性結構。若考題強調可解釋性與法遵，應說明 PCA 可作為輔助工具，但授信決策仍需保留可解釋特徵與模型說明。

---

### 8. SVD 與推薦系統有何關係？請以智能理財 Robo Advisor 為例說明。

**答題方向：** 使用者-商品矩陣、潛在因子、矩陣分解、推薦排序。

**詳細原理與解答：**

SVD 奇異值分解可將任意矩陣拆解為三個矩陣，常用於資料降維與潛在特徵分析。在推薦系統中，可以建立「客戶 × 金融商品」矩陣，矩陣元素代表客戶是否持有商品、點擊、申購、風險偏好或滿意度。SVD 可將這個矩陣拆解成客戶潛在偏好向量與商品潛在特徵向量。

以 Robo Advisor 為例，模型可根據客戶風險屬性、投資年限、過去互動與商品特徵，推估客戶可能適合的基金或理財商品。SVD 的價值在於能發現表面欄位看不到的潛在偏好，例如某群客戶偏好低波動債券型商品，另一群客戶偏好成長型商品。

但金融推薦不能只最大化點擊率或申購率，還必須加入適合度、風險揭露、商品限制、客戶承受能力與法遵規範。高分答案要指出：推薦系統在銀行中必須是「模型推薦 + 規則約束 + 人工/合規檢核」的混合架構。

---

### 9. 請說明最小平方法 OLS 與線性迴歸，並舉銀行房貸估價應用。

**答題方向：** 平方誤差、最佳擬合、可解釋、離群值限制。

**詳細原理與解答：**

線性迴歸是用一組線性權重描述輸入特徵與連續目標值之間的關係。最小平方法 OLS 的目標是找到一組參數，使所有樣本的預測值與真實值之間的平方誤差總和最小。

在房貸估價中，輸入特徵可包含房屋坪數、屋齡、地段、樓層、建物型態、周邊成交價、捷運距離等，目標變數是房屋估值或成交價格。線性迴歸的優點是可解釋性高，例如可以說明坪數或地段對估值的影響方向與幅度，適合需要透明度的金融場景。

限制是它假設關係近似線性，若房價受到複雜非線性因素影響，可能表現不足；另外 OLS 對離群值敏感，極端豪宅或特殊物件可能扭曲模型。實務上可加入特徵轉換、穩健迴歸、樹模型或集成模型進行比較。

---

## 第三部分：優化、損失函數與訓練穩定性

### 10. 請比較 MSE、MAE、Huber Loss 在銀行迴歸任務中的差異。

**答題方向：** 大誤差懲罰、異常值、穩健性。

**詳細原理與解答：**

MSE 均方誤差會將誤差平方，因此對大誤差懲罰較重，適合誤差分佈穩定且希望特別壓低大錯誤的場景，例如房屋估價或業務量預測。但若資料中有極端值，MSE 可能被少數異常樣本主導。

MAE 平均絕對誤差對每個誤差給予線性懲罰，對離群值較穩健，也較容易解釋，例如平均估價誤差多少元。但 MAE 在數學上部分位置不可導，模型訓練可能較不平滑。

Huber Loss 則結合 MSE 與 MAE，對小誤差使用平方懲罰，對大誤差使用線性懲罰，因此兼具穩定訓練與抗異常值能力。銀行資料常有極端交易或特殊客戶，因此 Huber Loss 在部分迴歸任務中是合理選擇。

---

### 11. 請說明 Cross-Entropy Loss 為何常用於分類問題，並以客服意圖辨識為例。

**答題方向：** 機率分佈、分類、softmax、錯誤信心懲罰。

**詳細原理與解答：**

Cross-Entropy Loss 用來衡量模型預測機率分佈與真實標籤分佈的差異。分類模型通常輸出每個類別的機率，若模型對正確類別給高機率，loss 會低；若對錯誤類別非常有信心，loss 會很高。

在智能客服意圖辨識中，模型需要判斷客戶問題屬於哪一類，例如匯率查詢、信用卡帳單、貸款申請、分行資訊等。若客戶輸入「請問今天日幣匯率」，正確類別應是「匯率查詢」。如果模型把「信用卡帳單」預測為 0.9，Cross-Entropy 會給予很大懲罰，促使模型修正參數。

金融客服場景還需加入信心門檻。即使 Cross-Entropy 訓練出不錯模型，若預測最高機率未達門檻，系統仍應要求補充資訊或轉人工，避免錯誤金融資訊造成客訴或合規風險。

---

### 12. 請比較 GD、SGD、Mini-batch SGD 與 Adam 的差異。

**答題方向：** 更新資料量、穩定性、效率、深度學習。

**詳細原理與解答：**

Gradient Descent 每次使用全部訓練資料計算梯度，方向穩定但成本高，適合小資料或凸問題。SGD 每次使用單一樣本更新，速度快、記憶體需求低，但梯度噪聲大，收斂較不穩定。Mini-batch SGD 每次使用一小批資料，兼顧效率與穩定性，是深度學習最常見做法，也容易搭配 GPU 加速。

Adam 結合 Momentum 與 RMSprop，會估計梯度的一階動量與二階動量，自動調整不同參數的學習率。它通常收斂快、穩定性高，常用於 NLP、影像與深度學習模型。

在銀行 AI 中，若訓練文字客服模型或深度學習模型，常使用 Mini-batch SGD 或 Adam。若是簡單線性模型，則可能使用封閉解或傳統優化方法。高分答案需指出：優化器選擇會影響收斂速度、穩定性與最終模型表現，但仍需配合學習率、batch size、early stopping 與驗證集監控。

---

### 13. 若深度學習模型訓練不穩定，可能原因與解法為何？

**答題方向：** 學習率、梯度爆炸/消失、初始化、batch size、early stopping。

**詳細原理與解答：**

深度學習模型不穩定可能來自多種原因。第一，學習率過高會導致參數更新過大，在最小值附近震盪甚至發散；學習率過低則會收斂太慢。第二，深層網路可能出現梯度消失或梯度爆炸，使前面層學不到東西或參數變化過大。第三，權重初始化不當可能讓模型一開始就落在不良區域。第四，batch size 過小可能導致梯度估計噪聲過大。第五，資料分佈不穩或未標準化也會影響訓練。

解法包括學習率調整、使用 Adam 或 RMSprop、Gradient Clipping、Batch Normalization、合適初始化、資料標準化、Early Stopping，以及監控訓練集與驗證集 loss。

銀行場景中，模型穩定性很重要，因為客服、授信、防詐模型若不穩，會造成服務品質下降或風險誤判。因此部署前應以驗證集與測試集確認穩定性，上線後也要持續監控模型漂移。

---

## 第四部分：機器學習類型與常見演算法

### 14. 請比較監督式學習、非監督式學習與強化式學習，並舉土地銀行可能應用。

**答題方向：** 標籤、任務、應用場景。

**詳細原理與解答：**

監督式學習使用有標籤資料，目標是學習輸入 X 到輸出 Y 的映射，常見任務是分類與迴歸。土地銀行可用於違約預測、客服意圖分類、房屋估價、防詐分類等。

非監督式學習不依賴標籤，主要從資料中找結構，常見任務包括分群、降維與異常偵測。土地銀行可用於客戶分群、交易行為分群、異常交易探索、客服問題主題探索等。

強化式學習是模型與環境互動，根據回饋學習長期報酬最大化策略。銀行可能用於動態推薦、智能理財資產配置模擬、客服對話策略優化。但金融場景風險高，通常需要模擬環境、限制條件與人工監督。

高分答案要指出：不同學習類型取決於資料是否有標籤、任務是否需要預測或探索、以及是否涉及連續決策。

---

### 15. 請說明 Logistic Regression 在銀行違約預測中的原理、優點與限制。

**答題方向：** sigmoid、二元分類、機率輸出、可解釋。

**詳細原理與解答：**

Logistic Regression 是二元分類常用模型，會先計算線性分數 z = w^T x + b，再透過 sigmoid 函數將分數壓到 0 到 1 之間，作為某事件發生的機率。例如 P(違約=1|客戶特徵)。

在銀行違約預測中，輸入特徵可以包括收入、負債比、繳款紀錄、信用卡循環比例、職業、年齡、貸款金額等。模型輸出違約機率後，銀行可設定門檻進行風險分層。

優點是訓練快、可解釋性高、輸出機率容易用於風險管理，適合金融法遵需要說明原因的場景。限制是它主要學習線性決策邊界，對複雜非線性關係表現有限，且特徵工程品質會高度影響表現。

實務上可把 Logistic Regression 作為 baseline，再與 Random Forest、XGBoost、LightGBM 等模型比較。

---

### 16. 請比較 Decision Tree、Random Forest 與 Gradient Boosting 在授信風險模型中的適用性。

**答題方向：** 可解釋性、過擬合、集成、準確度。

**詳細原理與解答：**

Decision Tree 透過一連串條件分裂資料，例如負債比是否大於某門檻、過去是否逾期等。它容易理解，適合與業務單位溝通，但單一樹容易過擬合。

Random Forest 是 Bagging 集成方法，訓練多棵決策樹並平均或投票，能降低單一樹的變異，提高穩定性。缺點是解釋性比單一樹低，但仍可透過特徵重要度輔助解釋。

Gradient Boosting 如 XGBoost、LightGBM 是 Boosting 方法，逐步訓練新模型修正前一模型錯誤，通常在結構化資料上表現很強，適合授信風險、防詐、行銷轉換率預測等。但它調參較複雜，也需注意過擬合與可解釋性。

銀行實務上可採「Logistic Regression 作 baseline + 樹模型提升效能 + SHAP 解釋 + 人工覆核」的架構。

---

### 17. 支援向量機 SVM 的核心概念為何？適合用於哪些銀行資料？

**答題方向：** 最大間隔、kernel、高維、小中型資料。

**詳細原理與解答：**

SVM 的核心是尋找能最大化類別間隔的決策邊界。線性 SVM 適合資料可近似線性分隔的情況；若資料非線性，則可透過 kernel trick 將資料映射到高維空間，使其在高維中可分。

銀行應用中，SVM 可用於文字分類、文件分類、詐欺偵測或小中型高維資料。例如客服文字經 TF-IDF 向量化後形成高維稀疏特徵，SVM 常能有不錯表現。

限制是 SVM 在超大資料集上訓練成本較高，且模型結果不如 Logistic Regression 容易直接解釋成機率。若用於金融決策，仍需搭配機率校準、模型解釋與人工覆核。

---

### 18. K-means 分群如何用於土地銀行客戶分群？其限制為何？

**答題方向：** 無標籤、距離、K 值、標準化。

**詳細原理與解答：**

K-means 是非監督式分群方法，目標是將資料分成 K 群，使每個樣本到其群中心的距離總和最小。流程包括選擇 K、初始化中心點、分配樣本到最近中心、更新中心點，重複直到收斂。

土地銀行可用 K-means 進行客戶分群，例如依據存款餘額、交易頻率、數位通路使用、產品持有數、貸款需求、客服互動等特徵，分出高數位活躍客戶、房貸潛力客戶、財富管理客戶、低互動客戶等。

限制包括需事先指定 K、對尺度敏感、對離群值敏感、偏好球狀群集。因此分群前需標準化，並可用 elbow method、silhouette score 評估 K 值。分群後也要由業務解釋每群特徵，不能只拿群號直接決策。

---

### 19. 請說明異常偵測在金融防詐與 RPA 監控中的應用。

**答題方向：** 少數事件、無監督/半監督、交易異常、流程例外。

**詳細原理與解答：**

異常偵測用來找出與大多數資料模式不同的樣本。在金融防詐中，詐欺樣本通常很少且型態快速變化，因此除了監督式分類，也可使用異常偵測找出可疑交易，例如短時間多次轉帳、登入地點異常、金額遠高於歷史平均、交易時間不尋常等。

在 RPA 監控中，異常偵測可用於發現流程例外，例如報表數字突然異常、資料筆數與過去差距過大、某流程執行時間暴增、稅務申報欄位格式異常等。

常見方法包括統計門檻、Isolation Forest、One-Class SVM、Autoencoder 重建誤差等。高分答案要說明：異常偵測不等於直接判定詐欺，而是產生警示，後續需人工覆核與回饋標註，逐步改善模型。

---

### 20. 請說明推薦系統如何應用於智能理財，並比較內容式推薦與協同過濾。

**答題方向：** 使用者偏好、商品特徵、矩陣分解、適合度。

**詳細原理與解答：**

內容式推薦根據商品特徵與客戶偏好匹配，例如客戶偏好低風險、短天期、債券型商品，系統推薦相似特徵商品。協同過濾則根據相似客戶的行為推薦，例如與某客戶行為相近的人也購買某基金，因此推薦給該客戶。

在智能理財中，內容式推薦較容易解釋，也較符合風險屬性與商品適合度要求；協同過濾能發現潛在偏好，但可能受熱門商品偏誤與冷啟動問題影響。實務上常採混合式推薦，並加入規則約束，例如客戶風險等級不可低於商品風險等級。

銀行推薦系統不能只追求銷售或點擊率，還需兼顧投資適合度、風險揭露、公平性與合規，因此推薦結果應附上理由並保留紀錄。

---

## 第五部分：深度學習、NLP 與文件影像

### 21. CNN 如何用於銀行文件或票據影像辨識？

**答題方向：** 卷積、局部特徵、OCR、文件分類。

**詳細原理與解答：**

CNN 卷積神經網路適合處理影像資料，透過卷積核擷取局部特徵，如邊緣、紋理、形狀，再透過多層結構學習更高階的圖像表示。在銀行場景中，CNN 可用於票據影像辨識、身分證件辨識、表單分類、手寫欄位辨識與文件 OCR 品質提升。

例如在貸款申請流程中，客戶上傳財力證明、身分證件或申請書，系統可先用 CNN / OCR 辨識文件類型與欄位，再將結構化欄位送入授信流程或人工覆核。

高分答案需補充：金融文件辨識需要高準確率與可追溯性，對低信心欄位應轉人工覆核；同時需保護個資，控制影像資料存取權限，並保留處理紀錄。

---

### 22. RNN / LSTM / GRU 與 Transformer 在文字與序列資料上有何差異？

**答題方向：** 序列、長距依賴、注意力、平行化。

**詳細原理與解答：**

RNN 適合處理序列資料，會將前一時間點的狀態傳到下一時間點，但傳統 RNN 容易遇到梯度消失，難以記住長距資訊。LSTM 與 GRU 透過閘門機制改善長距依賴問題，常用於交易序列、語音與文字任務。

Transformer 則以 self-attention 為核心，可同時關注序列中不同位置的關係，平行化能力強，特別適合大規模 NLP 任務。智能客服、語意搜尋、文件摘要、法規問答與生成式 AI 多以 Transformer 架構為基礎。

在土地銀行智能客服中，若是早期簡單意圖分類，可用 TF-IDF + 傳統 ML；若要更好理解語意與上下文，可用 BERT 類 Transformer 模型；若要生成回答，則需更嚴格的知識庫 grounding 與風險控管，避免幻覺。

---

### 23. 請設計土地銀行智能客服 NLP 系統的完整流程。

**答題方向：** 斷詞、向量化、意圖分類、實體抽取、知識庫、評估、治理。

**詳細原理與解答：**

完整流程可分為資料、模型、服務與治理四層。

資料層：蒐集歷史客服問題、FAQ、金融商品說明、人工客服對話紀錄，進行個資遮蔽與標註。標註內容包括意圖類別、重要實體、是否需人工處理。

模型層：先進行中文斷詞或使用 Transformer tokenizer，將文字轉為向量。接著建立意圖分類模型，判斷客戶問題類別；用實體抽取辨識幣別、日期、商品名稱、分行名稱等資訊；再查詢知識庫或後端系統取得答案。

服務層：若模型信心高，直接回覆或提供功能選單；若信心低或問題涉及敏感金融判斷，轉人工客服。系統需記錄問題、模型信心、回覆結果與客戶滿意度。

治理層：定期檢查錯答率、熱門問題、轉人工比例、模型漂移與個資風險。若導入生成式 AI，應採 RAG 或知識庫檢索，並限制模型不可自行編造金融建議。

---

### 24. 生成式 AI 可如何用於銀行內部行政作業？風險與控制措施為何？

**答題方向：** 摘要、草稿、知識查詢、幻覺、個資、合規。

**詳細原理與解答：**

生成式 AI 可用於銀行內部行政作業，例如會議摘要、文件草稿、內部知識查詢、法規條文初步整理、客服知識庫維護、程式碼輔助與教育訓練。其價值在於提升作業效率與降低重複性文書負擔。

但金融業使用生成式 AI 面臨三大風險：第一，輸出內容可能不穩定、不精確或幻覺；第二，資料安全與隱私保護，不能把客戶個資或機密資料送入不受控系統；第三，合規性問題，模型輸出不能取代專業判斷或法遵審核。

控制措施包括建立內部封閉環境、資料分級、敏感資訊遮蔽、RAG 連接可信知識庫、輸出附來源、人工覆核、權限控管、使用紀錄留存與模型定期評估。

高分答案要強調：生成式 AI 在銀行應定位為「輔助工具」，不是自動決策者。

---

## 第六部分：建模流程、評估與調參

### 25. 請說明土地銀行若要建立企業戶授信風險模型，完整建模流程為何？

**答題方向：** 問題定義、資料、特徵、模型、評估、部署、治理。

**詳細原理與解答：**

第一步是定義業務問題，例如預測企業戶未來 12 個月是否違約，目標變數 Y 為是否違約。第二步盤點資料來源，包括內部授信資料、還款紀錄、財務報表、交易往來、擔保品資料，以及合法取得的外部資料，例如稅籍登記或公開商業資料。

第三步進行資料清理，包括缺失值、異常值、重複資料、欄位格式一致化與時間對齊。第四步做特徵工程，例如負債比、營收成長率、逾期次數、交易穩定度、產業風險、公司成立年數等。第五步切分 train / validation / test，避免時間洩漏。

第六步建立 baseline，例如 Logistic Regression，再比較 Random Forest、XGBoost、LightGBM 等模型。第七步用 AUC、PR-AUC、Recall、Precision、KS、校準曲線等評估。第八步選擇門檻，設計人工覆核流程。最後部署後監控模型漂移、違約率變化、族群公平性與法遵風險。

---

### 26. 為什麼金融防詐模型不能只看 Accuracy？應使用哪些指標？

**答題方向：** 不平衡資料、Recall、Precision、F1、PR-AUC、成本。

**詳細原理與解答：**

詐欺交易通常比例極低，假設 10,000 筆交易中只有 10 筆詐欺，模型若全部預測為正常，也有 99.9% accuracy，但完全沒有防詐能力。因此防詐模型不能只看 Accuracy。

更重要的指標包括 Recall、Precision、F1、ROC-AUC 與 PR-AUC。Recall 衡量真正詐欺被抓出的比例，對防止損失很重要；Precision 衡量被模型判定為詐欺中真的詐欺比例，關係到誤報與客戶體驗；F1 平衡 Precision 與 Recall；PR-AUC 在少數類別問題中比 ROC-AUC 更具參考價值。

金融實務還應加入成本敏感評估，例如漏抓一筆詐欺的損失、誤擋正常交易造成的客訴成本、人工覆核成本。最後應透過門檻調整，在風險控管與客戶體驗間取得平衡。

---

### 27. 請說明資料切分與交叉驗證的重要性，並解釋金融資料中的資料洩漏問題。

**答題方向：** train/validation/test、CV、time split、leakage。

**詳細原理與解答：**

資料切分是為了評估模型在未知資料上的泛化能力。訓練集用於學習參數，驗證集用於調參與模型選擇，測試集用於最終評估。交叉驗證則可在資料量有限時多次切分訓練與驗證，提高評估穩定性。

金融資料特別要注意時間順序。若用未來資料預測過去，會產生資料洩漏。例如用「未來三個月是否逾期」衍生出的欄位預測當下違約風險，模型在測試時會看似很準，但上線後無法取得該欄位。又例如標準化時若用全資料計算平均與標準差，也會讓測試集資訊滲入訓練流程。

因此，授信、防詐、投資等時間序列相關任務應採時間切分，並確保所有特徵在預測當下可取得。這是金融 AI 建模非常重要的考點。

---

### 28. 請比較 Grid Search、Random Search 與 Bayesian Optimization。

**答題方向：** 超參數、搜尋效率、成本。

**詳細原理與解答：**

Grid Search 會窮舉所有指定超參數組合，優點是簡單完整，缺點是計算成本高，尤其當超參數很多時效率很差。Random Search 從超參數空間隨機抽樣，通常在同樣計算成本下比 Grid Search 更有效，因為不是每個超參數都同等重要。

Bayesian Optimization 會根據過去試驗結果建立代理模型，預測哪些超參數組合更可能帶來好表現，再優先嘗試，適合模型訓練成本高的情境。

在銀行 AI 中，若模型較簡單或資料不大，可用 Grid Search；若參數空間大，可用 Random Search；若深度學習或大資料模型訓練成本高，可考慮 Bayesian Optimization。無論哪種方式，都應以驗證集或交叉驗證評估，避免只對測試集調參。

---

### 29. 何謂模型漂移？土地銀行智能客服或防詐模型如何監控漂移？

**答題方向：** data drift、concept drift、監控、再訓練。

**詳細原理與解答：**

模型漂移是指模型上線後，資料分佈或輸入與目標的關係發生變化，導致模型表現下降。Data drift 指輸入資料分佈改變，例如客戶問題從傳統業務轉向新產品；concept drift 指 X 與 Y 的關係改變，例如詐欺手法改變，使原本風險特徵不再有效。

智能客服可監控熱門問題變化、未知意圖比例、轉人工比例、錯答率、客戶滿意度與模型信心分佈。防詐模型可監控交易金額分佈、異常警示率、Precision、Recall、人工覆核結果與實際損失。

若發現漂移，應進行資料重新標註、模型再訓練、特徵更新、門檻調整或導入新規則。金融業還應保留版本管理與回滾機制，避免新模型上線造成不可控風險。

---

## 第七部分：治理、隱私、公平性與部署

### 30. 請說明銀行 AI 為何需要個資保護與資料最小化原則。

**答題方向：** 個資、目的限制、最小化、遮蔽、權限。

**詳細原理與解答：**

銀行資料高度敏感，包含身分資料、財務資料、交易紀錄、信用資料與投資偏好。AI 模型若不當使用資料，可能造成個資外洩、再識別、歧視或違反法規。因此資料保護是銀行 AI 的基本前提。

資料最小化原則要求只蒐集與使用達成目的所需的資料。例如建立客服意圖分類模型，不應使用完整身分證字號或帳戶餘額；建立授信模型時，也應清楚定義特徵使用目的與合法依據。

技術措施包括資料遮蔽、去識別化、泛化、分桶、權限控管、加密、存取紀錄、資料保存期限管理。治理措施包括資料使用審核、模型開發流程紀錄、第三方委外控管與內部稽核。

---

### 31. 請比較遮蔽、泛化、分桶、Noise Injection、k-anonymity、l-diversity 與 t-closeness。

**答題方向：** 隱私技術、再識別風險、資料可用性。

**詳細原理與解答：**

遮蔽是隱藏敏感欄位的一部分，例如身分證字號只顯示後四碼。泛化是降低資料精確度，例如將精確年齡改為年齡區間。分桶是把連續數值轉為區間，例如收入 58,000 元改為 50K-60K。Noise Injection 是加入隨機噪聲，使單筆資料難以精準還原，但保留整體統計特性。

k-anonymity 要求每筆資料至少與 k-1 筆資料在準識別欄位上相同，以降低再識別風險。l-diversity 進一步要求每個匿名群組中的敏感屬性有足夠多樣性，避免群組內敏感值過於一致。t-closeness 則要求群組內敏感屬性的分佈接近整體資料分佈，降低透過分佈差異推測個人資訊的風險。

這些方法都需要在隱私與資料可用性之間取捨。保護太弱會有外洩風險，保護太強則可能降低模型準確度。

---

### 32. 請說明演算法偏見在銀行授信模型中的來源與改善方式。

**答題方向：** 歷史偏見、樣本不平衡、代理變數、公平性指標。

**詳細原理與解答：**

授信模型的偏見可能來自歷史資料、樣本不平衡、標籤偏誤與代理變數。若過去授信決策本身存在偏差，模型學習歷史資料後也可能複製偏差。若某些族群資料量較少，模型對該族群預測可能較不穩定。某些看似中性的欄位，如地區、職業、收入型態，也可能間接反映敏感屬性。

改善方式包括資料層面的重新取樣、SMOTE、類別重加權；模型層面的公平性約束與去偏訓練；評估層面分族群計算錯誤率、拒絕率、召回率、False Positive Rate、False Negative Rate；治理層面建立公平性審查、人工覆核、申訴機制與模型文件。

高分答案要強調：公平性不是要求每個人都得到同樣結果，而是避免模型在不合理或違法的因素上造成系統性不利影響。

---

### 33. 金融 AI 模型為何需要可解釋性？請舉三種可解釋方法。

**答題方向：** 客戶權益、法遵、模型信任、feature importance、SHAP、LIME。

**詳細原理與解答：**

金融 AI 常涉及授信、投資建議、防詐、客戶分群等高影響決策。若模型無法解釋，銀行難以向客戶、主管機關或內部風控單位說明決策原因，也難以發現模型是否使用不合理特徵。

常見可解釋方法包括：第一，使用可解釋模型，如 Logistic Regression 或 Decision Tree；第二，使用特徵重要度，說明哪些特徵對模型整體預測影響最大；第三，使用 SHAP 或 LIME 等局部解釋方法，說明單一客戶被判定為高風險的主要原因。

例如授信模型拒絕某客戶時，銀行應能說明主要因素可能是負債比過高、近期逾期紀錄、收入不穩定等，而不是無法解釋的黑箱分數。高分答案還應補充：可解釋性應搭配模型文件、版本控管與人工覆核。

---

### 34. 若土地銀行使用第三方 AI 模型或雲端服務，應注意哪些風險？

**答題方向：** 供應商風險、資料安全、合規、可稽核、SLA。

**詳細原理與解答：**

使用第三方 AI 模型或雲端服務時，首先要評估資料安全與隱私保護，確認客戶個資是否會離開銀行控制環境、是否加密、是否被第三方用於訓練模型。第二要評估合規風險，包括金融監理要求、委外管理、資料保存地點與跨境傳輸。第三要評估模型可稽核性，例如是否能提供模型文件、測試結果、更新紀錄與解釋機制。

此外，也要注意供應商穩定性、服務可用性、SLA、資安事件通報、退出機制與資料刪除機制。若是生成式 AI，還需限制幻覺、敏感資料輸入與不當輸出。

銀行不應把核心責任完全外包給廠商。即使模型由第三方提供，最終服務責任與客戶權益仍由銀行承擔。

---

### 35. 請設計「RPA + ML」用於報表產製與異常檢核的流程。

**答題方向：** RPA、自動化、資料驗證、異常偵測、人工覆核。

**詳細原理與解答：**

流程可分成五步。第一，RPA 自動登入內部系統或讀取指定資料來源，擷取每日業務資料。第二，ETL 流程進行欄位標準化、格式檢查、缺失值處理與重複資料移除。第三，產製報表與視覺化圖表，例如每日交易量、貸款申請數、客服量、異常件數。

第四，加入機器學習或統計異常偵測，例如比較今日數值與過去 30 日平均與標準差，或使用 Isolation Forest 偵測異常分布。第五，若發現異常，例如某分行數字突然暴增、欄位缺失率異常、報表總額不一致，系統發出警示並轉人工覆核。

治理上需保留 RPA 執行紀錄、資料版本、異常判斷原因與人工處理結果。這些結果可再回饋模型，使異常偵測更精準。

---

### 36. 請用 Python pseudo code 說明如何建立客服意圖分類模型的基本流程。

**答題方向：** 資料讀取、切分、向量化、訓練、評估、預測。

**詳細原理與解答：**

以下可作為申論題中的程式流程說明，不一定要背完整語法，但要能說出步驟。

```python
# 1. 載入資料：text 是客戶問題，label 是意圖類別
import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.linear_model import LogisticRegression
from sklearn.pipeline import Pipeline
from sklearn.metrics import classification_report

# 假設資料欄位包含 text, label
df = pd.read_csv('customer_service_intent.csv')

# 2. 切分資料，避免用測試集調參
X_train, X_test, y_train, y_test = train_test_split(
    df['text'], df['label'], test_size=0.2, random_state=42, stratify=df['label']
)

# 3. 建立流程：文字向量化 + 分類模型
model = Pipeline([
    ('tfidf', TfidfVectorizer(max_features=5000, ngram_range=(1, 2))),
    ('clf', LogisticRegression(max_iter=1000, class_weight='balanced'))
])

# 4. 訓練模型
model.fit(X_train, y_train)

# 5. 評估模型
pred = model.predict(X_test)
print(classification_report(y_test, pred))

# 6. 推論新問題
question = ['請問今天美金匯率是多少']
intent = model.predict(question)
prob = model.predict_proba(question).max()

# 7. 若信心不足，轉人工或要求補充資訊
if prob < 0.6:
    print('轉人工客服')
else:
    print('意圖類別:', intent[0])
```

這段流程涵蓋文字資料、特徵工程、模型訓練、評估與低信心轉人工。若要更進階，可改用 BERT / Transformer embedding，並加入知識庫檢索與錯答回訓機制。

---

## 考前總結：36 題要背的不是答案，而是作答骨架

遇到土地銀行 AI 申論題，請一律用以下順序思考：

```text
銀行場景 → 任務類型 → 資料來源 → 特徵工程 → 模型選擇 → 評估指標 → 部署監控 → 治理風險
```

只要你能把每題都寫回這條主線，答案就會比單純背模型名稱更像真正的 AI 人員。🔥

---

## 參考來源

1. 附件 PDF：《AI應用規劃師（中級）學習指引－科目3 機器學習技術與應用》。
2. 土地銀行「永續發展－客戶權益－數位金融」頁面公開揭露：智能客服、RPA、大數據分析、智能理財等資訊。
3. 金融監督管理委員會 2025-05-20「金融業應用人工智慧（AI）調查結果」。
